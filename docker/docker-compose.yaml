version: '3'

networks:
  api-gateway-net:
  profiles-net:

services:
  api-gateway:
    env_file:
      - .env-api-gateway
    image: autobidscloud/auto-bids:api-gateway
    ports:
      - "4000:4000"
    networks:
      - api-gateway-net
      - profiles-net

  profiles:
    env_file:
      - .env-profiles
    image: autobidscloud/auto-bids:profiles
    ports:
      - "4100:4100"
    networks:
      - api-gateway-net
      - profiles-net

